# API Gateway Requirements

# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# gRPC
grpcio==1.59.0
grpcio-tools==1.59.0
protobuf==4.25.0

# Authentication
python-jose[cryptography]==3.3.0
# passlib 1.7.4 is incompatible with bcrypt>=4.1; use bcrypt directly
bcrypt>=4.1
# Auth user store: without this the store silently falls back to
# in-memory and every gateway restart wipes all registered users
redis>=5.0

# Rate limiting
slowapi==0.1.9

# OpenTelemetry
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0
opentelemetry-instrumentation-grpc==0.42b0
opentelemetry-exporter-otlp-proto-grpc==1.21.0
opentelemetry-exporter-jaeger==1.21.0

# Prometheus metrics
prometheus-client==0.19.0

# WebSocket support
websockets==12.0

# Kafka client (optional)
kafka-python==2.0.2

# Utilities
pydantic==2.4.2
python-dotenv==1.0.0
